// Package terminology will contain terminology (value set / code system)
// expansion and code lookup logic.
//
// The package exists to preserve the architectural boundary:
// terminology sits below the registry and depends only on the model.
package terminology
